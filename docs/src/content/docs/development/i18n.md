---
title: Internationalization
description: How translations work and how to add new text.
---

Stellar Explorer supports 9 languages using [next-intl](https://next-intl.dev/).

## Supported Locales

| Code | Language |
|---|---|
| `en` | English |
| `es` | Spanish |
| `pt` | Portuguese |
| `fr` | French |
| `de` | German |
| `zh` | Chinese |
| `ja` | Japanese |
| `ko` | Korean |
| `it` | Italian |

## Translation Files

Translation files are in `messages/*.json`. Each file contains the same keys with translated values.

```
messages/
  en.json
  es.json
  pt.json
  ...
```

## Adding New Text

When adding UI text, you must update **all 9 translation files**. CI validates that all files have identical key counts.

1. Add the key to `messages/en.json`
2. Add the same key with translated values to all other files
3. Use the key in your component:

```tsx
import { useTranslations } from "next-intl";

function MyComponent() {
  const t = useTranslations("section");
  return <p>{t("myKey")}</p>;
}
```
